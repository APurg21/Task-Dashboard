import TaskDashboard from "@/components/TaskDashboard";

// The original task board — tasks, capture, KB chat, deep planner, import.
export default function TasksPage() {
  return <TaskDashboard />;
}
